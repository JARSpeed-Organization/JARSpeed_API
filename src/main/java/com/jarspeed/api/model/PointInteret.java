package com.jarspeed.api.model;

/**
 * Entité représentant un point d'intérêt sur un parcours sportif.
 * <p>
 * Cette classe est une entité JPA qui définit la structure de la table
 * des points d'intérêt dans la base de données. Elle inclut des informations
 * comme la position (latitude et longitude), une description, et le parcours
 * auquel le point est associé.
 * </p>
 */
//@Entity
public class PointInteret {
    /**
     * id du point d'intérêt.
     */
    private Long id;
    // autres attributs, getters et setters
}
